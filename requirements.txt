# Core inference engine. vLLM pins a compatible torch; let it manage torch on Colab.
# If a fresh Colab already has a torch that conflicts, restart runtime after install.
vllm>=0.7.2
transformers>=4.49.0
qwen-vl-utils>=0.0.8
accelerate>=0.34.0
pillow>=10.0.0
pandas>=2.0.0

# Thai text + math normalization
pythainlp>=5.0.0

# Optional: local equivalence check during eval (NOT used for the official metric)
# math-verify[antlr4_13_2]>=0.5.0

# Data download
kaggle>=1.6.0
